[agent]
name = "file-processor"
description = "An agent that can read and process files"
version = "0.1.0"
model = "gpt-4"
temperature = 0.5
max_tokens = 1000

[agent.tools]
allowed = ["file_read", "file_list"]

[cli]
mode = "freeform"
description = "Ask to process or analyze files"
