[agent]
name = "test-error-no-api-key"
version = "1.0.0"
description = "Test agent for validating error handling when no API key available"

[model]
suggested = ["anthropic/claude-3.5-sonnet", "openai/gpt-4o"]

[defaults]
temperature = 0.7
