[agent]
name = "test-skills-embedding"
version = "1.0.0"
description = "Test agent for validating skills embedding in binary"

[model]
suggested = ["anthropic/claude-3.5-sonnet", "openai/gpt-4o"]

[defaults]
temperature = 0.7
