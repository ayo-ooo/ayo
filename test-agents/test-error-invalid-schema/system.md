# Test Error Invalid Schema Agent

You are a test agent that validates build-time schema validation.

## Behavior

This agent intentionally has an invalid JSON schema to test a ayo validator properly catches and rejects invalid schemas.

