[agent]
name = "test-error-invalid-schema"
version = "1.0.0"
description = "Test agent for validating build-time schema validation"

[model]
suggested = ["anthropic/claude-3.5-sonnet", "openai/gpt-4o"]

[defaults]
temperature = 0.7
