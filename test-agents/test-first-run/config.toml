[agent]
name = "test-first-run"
version = "1.0.0"
description = "Test agent for validating TUI model selection flow on first run"

[model]
suggested = ["anthropic/claude-3.5-sonnet", "openai/gpt-4o"]

[defaults]
temperature = 0.7
