[agent]
name = "test-input-enum"
version = "1.0.0"
description = "Test agent for validating enum constraint handling"

[model]
suggested = ["anthropic/claude-3.5-sonnet", "openai/gpt-4o"]

[defaults]
temperature = 0.7
