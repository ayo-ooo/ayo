[agent]
name = "test-hooks-payload"
version = "1.0.0"
description = "Test agent for validating hook JSON payload format"

[model]
suggested = ["anthropic/claude-3.5-sonnet", "openai/gpt-4o"]

[defaults]
temperature = 0.7
